image: node:latest

before_script:
  - yarn install

pages:
  stage: deploy
  script:
    - CI=true yarn run build
    - rm -rf public
    - mv build public
  artifacts:
    paths:
      - public 
  only:
    - develop